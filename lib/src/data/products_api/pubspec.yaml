name: products_api
description: The interface and models for an API providing access to products.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.4.1 <4.0.0"

# TODO: adjust dependency constraints
dependencies:
  equatable: ^2.0.5
  json_annotation: ^4.9.0
  meta: ^1.12.0
  uuid: ^4.4.0

  fridge_manager:
    path: ../../../..

dev_dependencies:
  build_runner: ^2.4.11
  json_serializable: ^6.8.0
  # test: ^1.21.4
